// Example of admin page
/*
app.get('/admin/dashboard', (req, res) => {
  // The logic to retrieve the dashboard data should go here.
  res.render('admin/dashboard', {
    title: 'Admin Dashboard',
    stats: { users: 10, devices: 25, alerts: 5 }
  });
});
*/
